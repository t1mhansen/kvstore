#include <gtest/gtest.h>

// Intentionally failing placeholder: proves the test binary builds, links,
// and is actually executed by ctest/CI (not silently skipped). Milestone 2
// replaces this with the first real storage-engine tests.
TEST(Skeleton, ReplaceWithRealTestsInMilestone2) {
  FAIL() << "No storage engine yet - this failure confirms the test harness runs.";
}
